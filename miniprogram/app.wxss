/* 同乐八字 - 全局样式（浅色 · 未来星河风） */
/* 主色：靛紫 #6D5DF6  电光青 #22D3EE  墨蓝近黑 #14152B  白底 #F5F6FB */

page {
  background: #F5F6FB;
  background-image: linear-gradient(180deg, #EEF0FB 0%, #F5F6FB 420rpx);
  background-repeat: no-repeat;
  background-size: 100% 100%;
  color: #14152B;
  font-size: 28rpx;
  line-height: 1.6;
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", sans-serif;
  --primary: #6D5DF6;
  --primary-deep: #4B3DD4;
  --cyan: #22D3EE;
  --pink: #EC5C8D;
  --gold: #C08A2B;
  --ink: #14152B;
  --ink-2: #5B5E78;
  --ink-3: #9A9CB5;
  --card: #FFFFFF;
  --line: rgba(20, 21, 60, 0.08);
  --fill: #F1F2FA;
  --tint: rgba(109, 93, 246, 0.10);
}

/* 现代标题字（去衬线、去宋体，偏未来感） */
.serif {
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", sans-serif;
  letter-spacing: 1rpx;
}

/* 卡片：白底柔投影 */
.card {
  background: var(--card);
  border-radius: 24rpx;
  border: 1rpx solid var(--line);
  box-shadow: 0 8rpx 30rpx rgba(20, 21, 60, 0.06);
}

/* 主按钮：靛紫→电光青渐变 */
.btn-primary {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #6D5DF6, #5B8DEF);
  color: #FFFFFF;
  border-radius: 20rpx;
  font-size: 34rpx;
  font-weight: 600;
  letter-spacing: 6rpx;
  line-height: 1;
  padding: 0;
  box-shadow: 0 12rpx 30rpx rgba(109, 93, 246, 0.32);
}
.btn-primary::after {
  border: none;
}
.btn-primary.button-hover {
  opacity: 0.88;
}

/* 五行配色（适配白底的深色版）：木火土金水 */
.wx-mu { color: #16A971; }
.wx-huo { color: #E85540; }
.wx-tu { color: #8C5E2A; }
.wx-jin { color: #D4A017; }
.wx-shui { color: #2E86E6; }

/* 五行实底（彩色圆点，配深字） */
.bg-mu { background: #34C88A; }
.bg-huo { background: #FF7A63; }
.bg-tu { background: #B07C34; }
.bg-jin { background: #E6B81F; }
.bg-shui { background: #5AA6F2; }

/* 文字辅助 */
.text-2 { color: var(--ink-2); }
.text-3 { color: var(--ink-3); }

/* LOGO 徽标（现代玻璃渐变） */
.seal {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #7B6BFF, #4B3DD4);
  color: #FFFFFF;
  border-radius: 22rpx;
  box-shadow: 0 10rpx 26rpx rgba(75, 61, 212, 0.35);
  font-weight: 700;
}
